# Copy to .env and fill in real values before running `docker compose up`.
# Used by docker-compose.yml for the backend service.

JWT_SECRET=
CORS_ALLOWED_ORIGINS=https://flooring.shahirjalal.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
